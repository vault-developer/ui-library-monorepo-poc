import React from 'react';
import {Button} from '@vault-developer/ui-button';

const Form = () => (
    <form>
        <Button style={{background: 'green'}}>HELLO1</Button>
    </form>
);
export {Form};